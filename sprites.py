class Assets(object):
    def __init__(self, choice):
        self.path = choice
        self.directory = directory
        self. 

    def return_path(self):
        self.directory = "data/themes/" + choice

